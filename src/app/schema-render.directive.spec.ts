import { SchemaRenderDirective } from './schema-render.directive';

describe('SchemaRenderDirective', () => {
  it('should create an instance', () => {
   // const directive = new SchemaRenderDirective();
   // expect(directive).toBeTruthy();
  });
});
